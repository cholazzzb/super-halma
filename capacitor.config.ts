import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.toro.superhalma',
  appName: 'super-halma',
  webDir: 'build/client'
};

export default config;
